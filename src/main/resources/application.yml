spring:
  application:
    name: gateway
  lifecycle:
    timeout-per-shutdown-phase: 20s

server:
  port: ${GATEWAY_HTTP_PORT:8080}
  shutdown: graceful

gateway:
  security:
    jwt:
      public-key: ${GATEWAY_JWT_PUBLIC_KEY:}
